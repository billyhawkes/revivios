{
  "name": "revivios",
  "devDependencies": {
    "@types/bun": "^1.3.1"
  },
  "private": true,
  "scripts": {
    "dev": "bun --hot src/index.ts",
    "start": "bun src/index.ts",
    "typecheck": "tsc --noEmit",
    "db:pull": "dotenv -f .env -- litestream restore -o ./persist/sqlite.db s3://kokobi-prod/db"
  },
  "type": "module"
}
